import { Markup } from 'telegraf';

export class ReplyKeyboards {
  // Главное меню
  static mainMenu() {
    return Markup.keyboard([
      ['🎬 Найти фильм', '⏱️ Ручной режим'],
      ['ℹ️ Помощь', '📊 Статус']
    ]).resize();
  }

  // Меню админа
  static adminMenu() {
    return Markup.keyboard([
      ['🎬 Найти фильм', '⏱️ Ручной режим'],
      ['🔴 Выключить', '🔄 Перезапустить', '🟢 Включить'],
      ['ℹ️ Помощь', '📊 Статус']
    ]).resize();
  }

  // Меню во время поиска
  static searchingMenu() {
    return Markup.keyboard([
      ['❌ Отменить поиск'],
      ['🔙 Главное меню']
    ]).resize();
  }

  // Меню в ручном режиме
  static manualModeMenu() {
    return Markup.keyboard([
      ['❌ Выйти из ручного режима'],
      ['🔙 Главное меню']
    ]).resize();
  }

  // Меню с действиями после поиска
  static afterSearchMenu() {
    return Markup.keyboard([
      ['🎬 Новый поиск', '⏱️ Ввести время'],
      ['🔙 Главное меню']
    ]).resize();
  }
}